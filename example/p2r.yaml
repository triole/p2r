---
sync_steps:
  - cmd:
      base: rsync
      args: ["-av", "--delete", "--chown={{.user}}:admins", "--exclude=acme"]
      debug_arg: -n
    local:
      folder: {{.workdir}}/conf
    remote:
      host: remote_machine
      folder: /etc/whatever

tunnel:
  local_port: 3333
  remote_port: 80
